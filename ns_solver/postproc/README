This folder contains a program that will use the 
state-vector contour plot produced by the super
sonic solver and generate a contour plot for the
Mach number and static pressure. The lower wall
static pressure is also written to the console.

Simply fill out the "inputfile.dat" then run 
"report_plots.exe"

